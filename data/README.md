# Data boundary

This directory documents future data ownership and may later contain small, original, non-executable synthetic fixtures. It must never become a dump of scraped, copyrighted, privileged, or real personal legal material.

The current frontend fixtures live in `client/src/lib/demo/seed.ts` so the browser demonstration works without file access, a database, or network.
